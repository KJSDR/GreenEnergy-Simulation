/**
 * TypeScript type definitions for the grid simulation
 * 
 * These types will mirror the Pydantic models from the backend
 * to ensure type safety across the full stack.
 */

// TODO: Define types for GridState, EnergySource, WeatherState, etc.
// These will be implemented in Phase 2 (Week 13-14)

export interface GridState {
  // Will be defined based on backend Pydantic models
}

export interface EnergySource {
  // Will be defined based on backend models
}

export interface WeatherState {
  // Will be defined based on backend models
}
